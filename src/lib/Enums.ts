export enum FileMode {
  Manual = 'manual',
  Automatic = 'automatic',
}

export enum FileType {
  Json = 'json',
  Po = 'po',
  Code = 'code',
}

export enum StatusBarItemType {
  PO,
  JSON,
  CODE,
}

// Enum for log types
export enum LogVerbosity {
  Verbose,
  Important,
}
