export enum FileMode {
  Manual = 'manual',
  Automatic = 'automatic',
}

export enum FileType {
  Json = 'json',
  Po = 'po',
  Code = 'code',
}

export enum TaskBarItemType {
  PO,
  JSON,
  CODE,
}
